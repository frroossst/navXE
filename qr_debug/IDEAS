1. Too many straight lines mess up the algo so, surround the qr code background with curvy lines?

2. Use segno to experiment with different styles of QR codes

3. Black background with white backgroudn?

4. Offload all qr code processing to external website or API, delete openCV dependencies

5. Create own software to DEBUG QR code
